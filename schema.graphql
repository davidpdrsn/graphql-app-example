schema {
  query: Query
  mutation: Mutation
}

type Query {
  users: [User!]! @juniper(ownership: "owned")
}

type Mutation {
  noop: Boolean!
}

type User {
  id: Int!
  name: String!
  country: Country! @juniper(ownership: "owned")
}

type Country {
  id: Int!
  name: String!
}
